# QUICKSTART — Claude Code 웹으로 개발 시작하기

## 0. 지금 바로 게임 확인 (1분)

이 폴더에서:
```bash
python3 -m http.server 8000
```
브라우저에서 `http://localhost:8000` → 캐릭터 18명 중 하나 고르고 방향키로 마을 돌아다니기.
**아이한테 먼저 보여주고 시작하세요.** 반응 보고 우선순위가 바뀝니다.

---

## 1. GitHub 저장소 만들기

Claude Code 웹은 **GitHub 저장소에서만** 동작합니다 (GitLab 등은 지원 안 됨).

```bash
cd number-village
git init
git add .
git commit -m "초기 스타터: Phase 0~1 완료"
gh repo create number-village --private --source=. --push
```
(`gh` CLI가 없으면 github.com에서 저장소를 만들고 파일을 업로드해도 됩니다.)

## 2. Claude Code 웹 연결

1. Claude 앱/웹에서 Claude Code 웹 세션을 열고 GitHub 계정을 연결합니다.
2. `number-village` 저장소를 선택합니다.
3. 세션은 격리된 클라우드 VM에서 돌아가며, 작업 결과는 브랜치와 PR로 올라옵니다.
   → 아빠가 PR을 확인하고 머지하면 됩니다. 실수해도 main은 안전합니다.

`CLAUDE.md`가 이미 저장소 루트에 있으므로 Claude Code가 실행 방법과 규칙을 자동으로 읽습니다.

---

## 3. 첫 프롬프트 (그대로 복사해서 붙여넣기)

```
docs/기획안.md 와 docs/진행상황.md 를 먼저 읽어줘.

이번 세션에서는 Phase 4(NPC 친구들)만 진행해줘. Phase 5 이후는 절대 건드리지 마.

만들 것:
1. src/npcs.js 를 새로 만든다.
2. CHARACTERS 목록에서 플레이어가 고른 캐릭터를 뺀 나머지로 NPC 24명을 마을에 스폰한다.
   (캐릭터가 18종이므로 일부는 중복 등장해도 된다)
3. 각 NPC는 마을 안 랜덤 목적지로 천천히 걸어가고, 도착하면 1~3초 쉬었다가 다음 목적지로 간다.
   걸을 때는 characters.js의 update(t, moving)를 moving=true로 호출한다.
4. 모든 NPC 머리 위에 이름표를 띄운다. Canvas로 텍스처를 그려서 Sprite로 만든다.
   (이미지 파일 다운로드 금지) 이름표는 항상 카메라를 향한다.
5. 플레이어가 NPC 6m 이내로 가면 그 NPC 머리 위에 "!" 표시가 뜬다.
6. 스페이스(또는 화면의 👋 버튼)를 누르면 가장 가까운 NPC가:
   - 말풍선으로 랜덤 대사를 3초간 보여주고 ("안녕!", "같이 놀자~", "오늘 날씨 좋다!" 등 8개 이상)
   - 폴짝 뛰거나 손을 흔든다
   - 화면 가운데에 "○○를 만났어요!" 토스트가 뜬다
7. 성능: 플레이어에게서 45m 넘게 떨어진 NPC는 애니메이션 업데이트를 건너뛴다.

제약:
- 빌드 도구, npm 패키지, 외부 에셋 추가 금지
- main.js는 npcs.js를 불러다 쓰기만 하고 최대한 짧게 유지
- 작업이 끝나면 python3 -m http.server 로 띄워서 콘솔 에러가 없는지 확인하고,
  docs/진행상황.md 를 갱신한 뒤 "Phase 4: NPC 친구들 추가" 로 커밋해줘
```

---

## 4. 다음 세션들에 쓸 프롬프트 (필요할 때 꺼내 쓰세요)

**Phase 3 마무리 — 마을 예쁘게 만들기**
```
docs/기획안.md 읽고, Phase 3 마무리만 해줘.
무지개 다리, 사탕 정원(알록달록 나무와 버섯), 숫자 조형물이 있는 언덕,
마을 경계 울타리를 world.js에 추가해줘.
그리고 성/집/나무에 간단한 원형 충돌 판정을 넣어서 통과하지 못하게 해줘
(충돌 목록은 buildWorld가 배열로 돌려주고 player.js에서 밀어내는 방식).
성능 위해 나무는 그림자를 끄고, 같은 geometry/material을 재사용해줘.
```

**Phase 5 — 친구 도감**
```
Phase 5만 진행해줘. src/save.js(localStorage)와 src/ui.js를 만든다.
만난 친구를 저장하고, 📖 버튼을 누르면 도감이 열린다.
만난 친구는 컬러 카드 + 이름, 못 만난 친구는 실루엣 + ?.
맨 위에 "18명 중 N명"을 아주 크게 표시.
글자는 최소로, 버튼은 크게. 7세가 혼자 쓸 수 있어야 한다.
```

**Phase 6 — 태블릿/모바일 + 사운드**
```
Phase 6만 진행해줘.
1) 왼쪽 아래 가상 조이스틱을 만들어 player.js의 joy 객체에 연결
2) Web Audio API로 코드에서 직접 소리를 만든다 (사운드 파일 다운로드 금지):
   걷기, 인사, 새 친구 만남 팡파레, 잔잔한 배경 멜로디
   음소거 버튼 필수
3) 태블릿에서 30fps 이상 나오도록 픽셀 비율과 그림자 해상도를 자동 조절
```

**아이 요청 처리용 (제일 자주 쓰게 될 프롬프트)**
```
아들이 요청한 걸 추가해줘: [여기에 아이 말을 그대로 적기]
기획안의 절대 원칙(실패 없음/글자 최소/조작 단순/예쁘게)은 지켜야 해.
작은 변경이면 바로 하고, 크면 뭘 할지 먼저 알려줘.
```

---

## 5. 배포 (아이가 태블릿으로 놀 수 있게)

빌드가 없으므로 GitHub Pages에 그대로 올라갑니다.

1. GitHub 저장소 → Settings → Pages
2. Source: `Deploy from a branch` → Branch: `main` / `(root)` → Save
3. 1~2분 뒤 `https://<아이디>.github.io/number-village/` 접속

태블릿 홈 화면에 추가해두면 앱처럼 실행됩니다.

---

## 6. 아이와 같이 할 때 팁

- **한 세션에 한 Phase.** 다 만들고 보여주지 말고, 조금씩 자주 보여주세요.
  "친구들이 걸어다녀!" 한 번이 완성된 게임보다 반응이 좋습니다.
- **아이를 기획자로.** 캐릭터 이름, 색깔, NPC 대사는 아이가 정하게 하세요.
  `src/characters.js` 맨 위 배열에 한 줄 추가하는 걸 아이 옆에서 같이 하면 됩니다.
- **아이 요청 목록을 쌓아두세요.** `docs/진행상황.md` 맨 아래에 적어두고 하나씩 처리.
- **캐릭터는 우리가 만든 오리지널**이라는 점을 아이한테 알려주세요.
  좋아하는 만화 캐릭터를 그대로 베끼는 대신 "우리 캐릭터"를 만드는 게
  아이한테도 훨씬 재밌고, 나중에 친구들한테 링크를 보여줘도 문제가 없습니다.
- **멀티플레이는 마지막에.** 서버가 붙는 순간 난이도가 10배가 됩니다.
  NPC 24명이 돌아다니면 아이는 이미 "온라인 게임"이라고 느낍니다.
